# Typescript Project Generator

An NPX command for generating typescript project templates (Currently supporting a REST API and a Progressive web app or PWA)


## Usage

```javascript
npx @ottercode/create-otter-proj
```

Then just follow the set up CLI
